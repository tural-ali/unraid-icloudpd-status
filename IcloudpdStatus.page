Menu="Dashboard:0"
Cond="version_compare(parse_ini_file('/etc/unraid-version')['version'],'6.11.9','>')"
---
<?PHP
require_once '/usr/local/emhttp/plugins/icloudpd-status/widget.php';

if (!function_exists('ipdw_render_tile')) {
  function ipdw_render_tile() {
    $style = "<style>"
      . ".ipdw{font-size:12px;line-height:1.4}"
      . ".ipdw-list{display:grid;gap:9px}"
      . ".ipdw-overview{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));padding:9px 10px;border-radius:9px;background:#f3f5f7}"
      . ".black .ipdw-overview{background:#25282c}"
      . ".ipdw-overview div{display:grid;gap:1px;min-width:0;padding:0 8px}"
      . ".ipdw-overview div+div{border-left:1px solid #dce2e8}"
      . ".black .ipdw-overview div+div{border-color:#3a4047}"
      . ".ipdw-overview b{font-size:14px;line-height:1.2;font-variant-numeric:tabular-nums;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}"
      . ".ipdw-overview span{font-size:9px;color:#657180;white-space:nowrap}"
      . ".black .ipdw-overview span{color:#aab2bd}"
      . ".ipdw-archives{display:grid;gap:8px}"
      . ".ipdw-card{overflow:hidden;border:1px solid #d8dde5;border-left:4px solid #94a3b8;border-radius:11px;background:rgba(255,255,255,.35)}"
      . ".ipdw-card-active{border-left-color:#2563eb}"
      . ".ipdw-card-complete{border-left-color:#16a34a}"
      . ".ipdw-card-bad{border-left-color:#dc2626}"
      . ".ipdw-card-idle{border-left-color:#d97706}"
      . ".black .ipdw-card{border-top-color:#383838;border-right-color:#383838;border-bottom-color:#383838;background:rgba(255,255,255,.02)}"
      . ".ipdw-card-summary{display:block;list-style:none;cursor:pointer;padding:10px 12px 8px}"
      . ".ipdw-card-summary::-webkit-details-marker{display:none}"
      . ".ipdw-card[open]>.ipdw-card-summary{border-bottom:1px solid #dce2e8}"
      . ".black .ipdw-card[open]>.ipdw-card-summary{border-color:#3a4047}"
      . ".ipdw-instance div{display:grid;gap:1px}"
      . ".ipdw-instance b{font-size:13px}"
      . ".ipdw-instance span{font-size:10px;color:#657180}"
      . ".black .ipdw-instance span{color:#aab2bd}"
      . ".ipdw-auth{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 10px;margin-bottom:9px;border-radius:8px;background:#fee2e2;color:#991b1b}"
      . ".ipdw-auth div{display:grid;gap:1px}"
      . ".ipdw-auth span{font-size:10px}"
      . ".ipdw-auth button{border:0;border-radius:6px;padding:6px 9px;background:#b91c1c;color:#fff;cursor:pointer;white-space:nowrap}"
      . ".ipdw-phase{display:block;margin-top:4px;font-size:10px;font-weight:600;color:#4f5d6b}"
      . ".black .ipdw-phase{color:#b6bec8}"
      . ".ipdw-summary-progress{display:flex;align-items:baseline;justify-content:space-between;gap:12px;margin-top:5px;font-variant-numeric:tabular-nums}"
      . ".ipdw-summary-progress strong{font-size:23px;line-height:1;font-weight:750;letter-spacing:-.5px}"
      . ".ipdw-summary-progress span{font-size:10px;color:#4f5d6b;text-align:right}"
      . ".black .ipdw-summary-progress span{color:#b6bec8}"
      . ".ipdw-bar{height:10px;border-radius:999px;background:#e2e7ed;overflow:hidden;margin:5px 0}"
      . ".black .ipdw-bar{background:#2b2f34}"
      . ".ipdw-fill{height:100%;border-radius:999px;background:linear-gradient(90deg,#2563eb,#3b82f6);transition:width .9s ease}"
      . ".ipdw-summary-foot{display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap;font-size:10px;color:#4f5d6b;font-variant-numeric:tabular-nums}"
      . ".black .ipdw-summary-foot{color:#c1c7cf}"
      . ".ipdw-summary-foot>span:first-child b{color:inherit}"
      . ".ipdw-toggle{margin-top:6px;padding-top:6px;border-top:1px solid #e3e7ec;font-size:9.5px;font-weight:600;color:#657180}"
      . ".black .ipdw-toggle{border-color:#343a40;color:#aab2bd}"
      . ".ipdw-toggle i{width:11px;font-size:8px}"
      . ".ipdw-hide{display:none}"
      . ".ipdw-card[open] .ipdw-show{display:none}"
      . ".ipdw-card[open] .ipdw-hide{display:inline}"
      . ".ipdw-expanded{padding:10px 12px 12px}"
      . ".ipdw-expanded-stats{display:grid;gap:4px;padding:8px 10px;margin-bottom:8px;border-radius:8px;background:#f3f5f7;font-size:10px;font-variant-numeric:tabular-nums}"
      . ".black .ipdw-expanded-stats{background:#25282c}"
      . ".ipdw-expanded-stats div{display:flex;justify-content:space-between;gap:10px}"
      . ".ipdw-expanded-stats span{color:#657180}"
      . ".black .ipdw-expanded-stats span{color:#aab2bd}"
      . ".ipdw-expanded-stats b{text-align:right;font-weight:600}"
      . ".ipdw-groups{display:grid;grid-template-columns:1fr 1fr;gap:0;padding:8px 10px;border-radius:9px;background:#f3f5f7}"
      . ".black .ipdw-groups{background:#25282c}"
      . ".ipdw-group{min-width:0;padding:0 5px}"
      . ".ipdw-group+.ipdw-group{border-left:1px solid #dce2e8;padding-left:12px}"
      . ".black .ipdw-group+.ipdw-group{border-color:#3a4047}"
      . ".ipdw-group h4{margin:0 0 5px;font-size:9.5px;text-transform:uppercase;letter-spacing:.55px;color:#657180}"
      . ".black .ipdw-group h4{color:#aab2bd}"
      . ".ipdw-big-value{display:flex;align-items:center;gap:6px;white-space:nowrap}"
      . ".ipdw-big-value i{color:#2563eb}"
      . ".ipdw-big-value b{font-size:14px;font-variant-numeric:tabular-nums}"
      . ".ipdw-sparkline{display:block;width:100%;height:26px;margin:3px 0 1px;overflow:visible}"
      . ".ipdw-sparkline polygon{fill:rgba(37,99,235,.1)}"
      . ".ipdw-sparkline polyline{fill:none;stroke:#2563eb;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;vector-effect:non-scaling-stroke}"
      . ".ipdw-sparkline-empty{height:26px;display:flex;align-items:center;margin:3px 0 1px;font-size:9px;color:#7a8592}"
      . ".black .ipdw-sparkline polygon{fill:rgba(59,130,246,.14)}"
      . ".black .ipdw-sparkline-empty{color:#8f98a3}"
      . ".ipdw-secondary{display:flex;justify-content:space-between;gap:5px;margin-top:2px;font-size:9px;color:#5b6673}"
      . ".black .ipdw-secondary{color:#b6bec8}"
      . ".ipdw-status-rows{display:grid;gap:3px;margin-top:7px;font-size:9.5px}"
      . ".ipdw-status-rows div{display:grid;grid-template-columns:90px max-content;align-items:center;justify-content:start;gap:4px}"
      . ".ipdw-status-rows span{color:#5b6673;white-space:nowrap}"
      . ".black .ipdw-status-rows span{color:#b6bec8}"
      . ".ipdw-status-rows span i{width:10px;color:#16a34a}"
      . ".ipdw-status-rows span.ipdw-row-bad i{color:#dc2626}"
      . ".ipdw-status-rows b{font-weight:600;white-space:nowrap}"
      . ".ipdw-pill{display:inline-flex;align-items:center;gap:5px;padding:3px 7px;border-radius:999px;font-size:10px;font-weight:700}"
      . ".ipdw-pill i{width:6px;height:6px;border-radius:50%;background:currentColor}"
      . ".ipdw-pill-good{color:#15803d;background:#dcfce7}"
      . ".ipdw-pill-warn{color:#a16207;background:#fef3c7}"
      . ".ipdw-pill-bad{color:#b91c1c;background:#fee2e2}"
      . ".ipdw-details{margin-top:8px;border-top:1px solid #dce2e8}"
      . ".black .ipdw-details{border-color:#3a4047}"
      . ".ipdw-details summary{cursor:pointer;padding:8px 0 6px;font-size:10px;font-weight:600;color:#4f5d6b}"
      . ".ipdw-details summary:hover{color:#2563eb}"
      . ".black .ipdw-details summary{color:#b6bec8}"
      . ".ipdw-detail-grid{display:grid;grid-template-columns:1fr auto;gap:5px 12px;padding:1px 0 8px;font-size:10px;font-variant-numeric:tabular-nums}"
      . ".ipdw-detail-grid span{color:#657180}"
      . ".black .ipdw-detail-grid span{color:#aab2bd}"
      . ".ipdw-detail-grid b{text-align:right;font-weight:600}"
      . ".ipdw-empty{padding:10px;color:#657180}"
      . "</style>";

    $script = "<script>"
      . "(function(){"
      . "window.ipdwOpenAuthTerminal=function(name){"
      . "var form=document.createElement('form');form.method='POST';form.action='/plugins/icloudpd-status/auth-terminal.php';form.target='_blank';"
      . "var input=document.createElement('input');input.type='hidden';input.name='name';input.value=name;form.appendChild(input);"
      . "var token=document.createElement('input');token.type='hidden';token.name='csrf_token';token.value=typeof csrf_token==='string'?csrf_token:'';form.appendChild(token);"
      . "document.body.appendChild(form);form.submit();form.remove();"
      . "};"
      . "if(window.ipdwTimer){return;}"
      . "function poll(){jQuery.get('/plugins/icloudpd-status/widget-poll.php',function(markup){"
      . "var parsed=new DOMParser().parseFromString(markup,'text/html');"
      . "var fresh=parsed.querySelector('.ipdw-list')||parsed.querySelector('.ipdw-empty');"
      . "var body=document.getElementById('ipdw-body');"
      . "if(fresh&&body){"
      . "var current=body.querySelectorAll('details.ipdw-card');var open={};var archiveOpen={};"
      . "current.forEach(function(card){var key=card.dataset.instance;if(card.open){open[key]=true;}var nested=card.querySelector('details.ipdw-details');if(nested&&nested.open){archiveOpen[key]=true;}});"
      . "if(current.length){fresh.querySelectorAll('details.ipdw-card').forEach(function(card){var key=card.dataset.instance;card.open=!!open[key];var nested=card.querySelector('details.ipdw-details');if(nested){nested.open=!!archiveOpen[key];}});}"
      . "body.replaceChildren(fresh);"
      . "}"
      . "});}"
      . "window.ipdwTimer=setInterval(poll,15000);"
      . "})();"
      . "</script>";

    $body = "<div class='ipdw'><div id='ipdw-body'>" . ipdw_render_body() . "</div></div>";

    return "<tbody title='All iCloudPD archive download instances'>"
      . "<tr><td><i class='fa fa-cloud-download' style='margin-right:6px'></i>iCloudPD Archives</td></tr>"
      . "<tr><td>" . $style . $body . $script . "</td></tr>"
      . "</tbody>";
  }
}

$ipdw_plugin = 'icloudpd-status';
$mytiles[$ipdw_plugin]['column3'] = ipdw_render_tile();
?>
